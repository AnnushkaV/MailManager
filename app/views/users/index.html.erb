<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

  <% @users.each do |user| %>
      <tr>
        <td><%=link_to user.name, 'users#show' %></td>
        <td><%=link_to 'Send Message' %></td>
      </tr>
  <% end %>
</table>

<br />
<%= will_paginate %>
